import { expect } from "chai";
import { tick } from "../src/gameOfLife.mjs";
import { readFile, parseFile, decode, encode } from "../src/RLEreader.mjs";


describe("Tick function returns an array", () => {
  it("", () => {
    expect(tick() instanceof Array).to.be.true;
  });
});

describe("", () => {
  it("", () => {

  });
});

describe("", () => {
  it("", () => {

  });
});

